<%= form_for(@user, local: true) do |f| %>
  <div class="form-group">
    <%= f.label "name:" %>
    <%= f.text_field :name, required: true %>
  </div>
  <div class="actions">
    <%= f.submit 'Sign Up' %>
  </div>
<% end %>